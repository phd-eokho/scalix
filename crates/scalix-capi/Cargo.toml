[package]
name = "scalix-capi"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "C ABI bindings for Scalix"

[lib]
name = "scalix"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
scalix-core = { path = "../scalix-core" }
